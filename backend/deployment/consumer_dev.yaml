apiVersion: v1
kind: Pod
metadata:
  name: kafka-consumer
  namespace: demo
spec:
  containers:
    - name: consumer
      image: public.ecr.aws/h7f0r1p2/demo-kube:latest
      imagePullPolicy: Always
      env:
        - name: KAFKA_BROKER
          value: "kafka-service.demo.svc.cluster.local:9092"
      resources:
        requests:
          memory: "128Mi"
          cpu: "250m"
        limits:
          memory: "256Mi"
          cpu: "500m"